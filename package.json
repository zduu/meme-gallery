{
  "name": "meme-gallery",
  "version": "2.0.0",
  "description": "表情包管理器 - 使用 Cloudflare Pages Functions + KV 存储",
  "scripts": {
    "dev": "wrangler pages dev . --kv MEME_GALLERY_KV --compatibility-date=2026-05-03",
    "preview": "wrangler pages dev . --compatibility-date=2026-05-03",
    "check": "node --check app.js && find functions -name '*.js' -print -exec sh -c 'node --input-type=module --check < \"$1\"' sh {} \\;",
    "test": "npm run check",
    "serve": "python3 -m http.server 8000"
  },
  "keywords": [
    "meme",
    "gallery",
    "cloudflare",
    "pages",
    "kv"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
